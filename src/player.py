# Write a class to hold player information, e.g. what room they are in
# currently.
class Player:
    def __init__(self, name,
      health, weapons, description,
       items=[]
     currentRoom=room['outside'],
       ):
        self.name = name
        self.currentRoom-currentRoom
        self.items


print(Player)